start: start-docker
  overmind start

start-docker:
  docker-compose up -d db redis

update:
  git pull
  if git diff --numstat orig_head | grep -q db/migrate; then \
    bundle exec rails db:migrate; \
  fi
